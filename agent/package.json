{
  "name": "modbus-site-agent",
  "version": "1.0.0",
  "description": "Edge agent: reads a local Modbus device and relays it to the central server over an outbound WebSocket tunnel (no VPN / public IP needed).",
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js"
  },
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "dotenv": "^17.4.2",
    "modbus-serial": "^8.0.25",
    "ws": "^8.18.0"
  }
}
